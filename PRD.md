# 알고리즘 학습 도우미 — PRD (Product Requirements Document)

문서 버전: v1.0 (초안) · 작성일: 2026-08-28
대상: AI 코딩 도구(에이전트)가 이 문서만 보고 바로 구현 가능하도록 작성됨

---

## 1. 목표와 성공 조건

### 1.1 목표
사용자가 **알고리즘 이름**을 입력하면, 해당 알고리즘의 설명·사용처·적용 가능한 문제·예시 코드·관련 알고리즘 목록을 한 화면에 출력한다. "관련된 알고리즘 보기" 버튼을 누르면 해당 알고리즘에 대해 동일한 동작(1번 행동)이 다시 실행된다.

사용자가 **문제(상황)**를 입력하면, 문제 설명과 "어떤 부분에서 어떤 알고리즘을 써야 하는지"에 대한 해설을 제공하고, 사용해야 할 알고리즘 이름의 버튼을 생성한다. 이 버튼을 누르면 해당 알고리즘에 대해 1번 행동(알고리즘 검색 결과 화면)이 그대로 실행된다.

즉 이 앱은 아래 두 가지 입력 경로가 결국 하나의 "알고리즘 상세 결과 화면"으로 수렴하는 단일 화면 구조를 가진다.

### 1.2 성공 조건 (Definition of Success)
- 알고리즘 이름을 입력했을 때, 그 알고리즘에 정확히 해당하는 설명/사용처/예시코드/적용 문제가 출력된다 (다른 알고리즘과 혼동되지 않음).
- 문제를 입력했을 때, 그 문제에 실제로 적용 가능한 알고리즘이 제시된다 (엉뚱한 알고리즘 추천 없음).
- 알고리즘 결과 화면의 "관련 알고리즘 보기" 버튼을 누르면 몇 단계를 거치더라도 항상 같은 형식의 결과 화면으로 이어진다 (순환 탐색이 끊기지 않음).
- 어떤 입력을 넣어도, 어떤 오류가 나도 화면이 하얗게 죽거나(White Screen) 멈추지 않고 항상 사용자에게 현재 상태를 알려준다.

### 1.3 범위 제약 (Out of Scope 요약)
- 로그인/회원가입 없음
- 결제 없음
- 데이터베이스(영구 저장소) 없음 — 모든 상태는 화면을 새로고침하면 초기화되는 휘발성 상태로만 존재
- 단일 화면(Single Page) — 라우팅/페이지 전환 없음
- 기능은 정확히 2개: **(A) 알고리즘 검색**, **(B) 문제 검색** (관련 알고리즘 보기 / 사용 알고리즘 버튼은 A를 재호출하는 것이므로 별도 기능이 아니라 A의 하위 동작임)

---

## 2. 사용자 스토리

**페르소나:** 컴퓨터공학과 재학생, 알고리즘 수업 및 코딩테스트를 준비 중.

- 나는 알고리즘 수업/문제풀이 도중 특정 알고리즘 용어가 떠오르지 않거나, 이름은 알아도 원리와 사용법이 헷갈릴 때가 많다. 그때마다 여러 사이트를 검색해서 찾아야 하는 게 번거롭다. **그래서** 알고리즘 이름만 입력하면 설명·사용처·예시코드·관련 알고리즘까지 한 번에 보고 싶다.
- 나는 문제를 보고도 "이 문제는 어떤 알고리즘으로 풀어야 하는지" 감이 잘 오지 않을 때가 많다. **그래서** 문제 내용을 입력하면 어떤 알고리즘을 써야 하는지, 문제의 어느 부분이 그 알고리즘을 써야 함을 암시하는지 알고 싶고, 바로 그 알고리즘의 상세 설명으로 이어서 보고 싶다.
- 나는 검색 결과나 예시 코드를 다른 곳(메모장, 코드 에디터)에 옮겨 적어야 할 때가 많다. **그래서** 각 출력 블록을 클릭 한 번으로 복사하고 싶다.
- 나는 이 도구를 나 혼자 빠르게 쓸 용도로 만드는 것이므로, 로그인이나 결과 저장 같은 부가 기능 없이 **입력 → 결과**가 즉시 나오는 가벼운 도구를 원한다.

---

## 3. 화면 명세

이 앱은 **단일 화면(Single Screen)**으로 구성되며, 상단 / 중단 / 하단 3개 영역으로 나뉜다.

### 3.1 상단 영역 — 입력부

상단 입력부는 **[알고리즘 검색] / [문제 검색] 두 개의 탭**으로 구성되며, 한 번에 하나의 탭만 활성화되어 화면에 보인다. 사용자는 탭을 클릭해 입력 모드를 전환할 수 있다.

**[알고리즘 검색] 탭**
| 요소 | 설명 |
|---|---|
| 알고리즘 검색 텍스트박스 | 알고리즘 이름을 입력. 지원 대상은 몇 가지 대표 알고리즘만이 아니라 학부 알고리즘 교육 및 코딩테스트에서 다루는 **사실상 모든 알고리즘**(정렬, 탐색, 그래프, 트리, 동적 계획법, 그리디, 분할 정복, 백트래킹/완전탐색, 문자열, 수학/정수론, 자료구조 기반 알고리즘 등 전 분야)을 아우르는 포괄적인 사전 정의 목록을 대상으로 한다. 사용자가 직접 스크롤하여 목록에서 선택할 수도 있고, 일부 문자만 입력하면 그 문자를 포함하는 알고리즘들이 자동완성 목록(드롭다운)으로 표시된다. 목록 항목은 마우스 클릭 또는 키보드 방향키+엔터로 선택 가능. |
| 알고리즘 검색 버튼 | 선택/입력된 알고리즘으로 검색을 실행 |

**[문제 검색] 탭**
| 요소 | 설명 |
|---|---|
| 문제 검색 텍스트박스 | 사용자가 자유 텍스트로 문제 상황/문제 설명을 입력하는 칸 (자동완성 없음, 자유 서술형) |
| 문제 검색 버튼 | 입력된 문제로 검색을 실행 |

- "관련된 알고리즘 보기" / "사용해야 되는 알고리즘" 버튼을 클릭하면 화면은 자동으로 **[알고리즘 검색] 탭**으로 전환되고, 해당 알고리즘의 상세 결과가 표시된다.
- 모바일 화면에서는 두 탭이 화면 폭에 맞춰 나란히 꽉 차게 표시되어, 손가락 터치만으로 쉽게 전환할 수 있다.

### 3.2 중단 영역 — 결과 출력부
결과 출력부는 검색 종류에 따라 두 가지 형태 중 하나로 렌더링된다. 결과 출력부 **최상단에는 항상 "상태 표시줄"**이 위치하며, 대기(idle) / 로딩(loading) / 성공(success) / 오류(error) 등 현재 상태를 문구와 아이콘으로 나타낸다 (세부 사항은 5장 참고).

**(A) 알고리즘 검색 결과일 경우, 위에서 아래 순서로:**
1. 알고리즘 설명
2. **알고리즘 난이도**(예: 하/중/상 또는 별점) 및 **왜 그 난이도로 판단했는지에 대한 설명**
3. 사용처 및 적용될 수 있는 문제
4. 사용처에 따른 예시 코드
   - 예시 코드는 **언어 탭**으로 전환하며 볼 수 있다 (예: `[C]` `[C++]` `[Java]` `[Python]` 등). 최소 **C, C++, Java, Python** 4개 언어는 항상 지원한다.
   - 최초 진입 시 **C++ 탭이 기본으로 활성화**되며, 이후 사용자가 다른 언어 탭을 선택하면 같은 세션 내에서는 그 선택이 유지된다(다음 검색 결과에도 동일 언어 탭이 기본으로 열림).
   - 언어 탭을 전환해도 같은 알고리즘·같은 사용처에 대한 코드만 바뀔 뿐, 설명/난이도/사용처 등 다른 카드의 내용에는 영향을 주지 않는다.

**(B) 문제 검색 결과일 경우, 위에서 아래 순서로:**
1. 문제 설명
2. **문제 난이도**(예: 하/중/상 또는 별점) 및 **왜 그 난이도로 판단했는지에 대한 설명**
3. 어떤 부분에서 어떤 알고리즘을 사용해야 하는지에 대한 해설
   - 해당 문제를 풀 수 있는 방법(알고리즘)이 **여러 개일 경우, 풀이별로 탭을 구성**하여 사용자가 모든 풀이를 전환하며 볼 수 있도록 한다 (예: `[풀이 1: 그리디]` `[풀이 2: 다이나믹 프로그래밍]` 탭).
   - **각 풀이 탭에는 해당 풀이의 시간복잡도를 반드시 표시**한다(예: `O(N log N)`). 풀이가 하나뿐인 경우에도 그 풀이의 시간복잡도는 동일하게 표시한다.
   - 풀이가 여러 개일 경우, 가장 효율적이거나 가장 널리 쓰이는 풀이의 탭이 기본으로 활성화된 상태에서 시작한다.

각 출력 블록(카드) 우측 상단에는 **복사 아이콘**이 있어, 클릭 시 해당 블록의 텍스트 전체를 클립보드로 복사한다. 문제 검색 결과에서 풀이 탭이 여러 개로 나뉜 경우, 복사 아이콘은 **현재 활성화된 탭의 내용만** 복사한다. 예시 코드 카드도 마찬가지로, 복사 아이콘 클릭 시 **현재 활성화된 언어 탭의 코드만** 복사된다.

### 3.3 하단 영역 — 다음 행동 버튼
- **(A) 알고리즘 검색 결과일 경우:** "관련된 알고리즘 보기" 버튼(들)이 표시된다. 관련 알고리즘이 여러 개면 각각 버튼으로 나열된다. 버튼 클릭 시 해당 알고리즘 이름으로 [알고리즘 검색] 탭에서 검색을 다시 실행한 것과 동일하게 동작한다 (=1번 행동 재실행, 탭은 자동으로 [알고리즘 검색]으로 전환됨).
- **(B) 문제 검색 결과일 경우:** "사용해야 되는 알고리즘" 버튼이 표시된다. 풀이가 여러 개라 탭으로 나뉜 경우, 이 버튼은 **현재 활성화된 탭에 해당하는 알고리즘**을 가리키며, 탭을 전환하면 버튼에 표시되는 알고리즘 이름도 함께 바뀐다. 버튼 클릭 시 해당 알고리즘 이름으로 [알고리즘 검색]을 실행한 것과 동일하게 동작하며, 화면은 [알고리즘 검색] 탭 및 (A) 결과 형태로 자동 전환된다.

### 3.4 로딩 상태의 레이아웃
처리 중(로딩) 상태일 때는 **중단 결과 출력부 + 하단 버튼 영역 전체**가 로딩 표시(스켈레톤 또는 스피너 오버레이)로 덮인다. 즉 로딩 표시는 상태 표시줄 아래에 있는 나머지 모든 칸을 차지한다.

---

## 4. 기능 명세

이 앱의 기능은 총 2개다. 두 기능 모두 아래 공통 파이프라인(입력 → 검증 → 처리 → 출력)을 따른다.

### 기능 A. 알고리즘 검색 → 상세 정보 출력
- **입력:** 사용자가 [알고리즘 검색] 탭에서 텍스트박스에 값을 입력(또는 자동완성 목록에서 선택)하고 검색 버튼을 클릭하거나 엔터를 누른다. 또는 다른 결과 화면의 "관련된 알고리즘 보기" / "사용해야 되는 알고리즘" 버튼을 클릭해도 동일 입력으로 간주되며, 이 경우 화면은 [알고리즘 검색] 탭으로 자동 전환된다.
- **검증:** 입력값이 비어 있지 않은지 확인. 자동완성 목록에 존재하는 알고리즘인지(사전 정의된 유효 알고리즘 집합에 포함되는지) 확인.
- **처리:** 검증을 통과하면 AI에게 해당 알고리즘에 대한 설명/난이도 및 난이도 판단 근거/사용처/적용 문제/**C, C++, Java, Python 등 언어별 예시코드**/관련 알고리즘 목록을 요청한다.
- **출력:** 알고리즘 설명, 알고리즘 난이도 및 난이도 판단 근거, 사용처 및 적용될 수 있는 문제, 사용처에 따른 예시 코드(**언어 탭으로 전환 가능, 최소 C/C++/Java/Python 지원**)가 순서대로 렌더링되고, 관련된 알고리즘 보기 버튼(들)이 생성된다.

### 기능 B. 문제 검색 → 알고리즘 매칭 및 해설
- **입력:** 사용자가 [문제 검색] 탭에서 텍스트박스에 문제 내용을 자유 서술형으로 입력하고 검색 버튼을 클릭하거나 엔터를 누른다.
- **검증:** 입력값이 비어 있지 않은지, 최소 의미를 가진 텍스트인지(공백/특수문자만으로 이루어지지 않았는지) 확인.
- **처리:** AI에게 해당 문제를 분석해 문제 난이도 및 판단 근거, 적용 가능한 풀이(알고리즘)들과 그 근거(문제의 어느 부분이 어떤 알고리즘을 암시하는지), 그리고 풀이가 여러 개인 경우 풀이별 시간복잡도를 요청한다.
- **출력:** 문제 설명, 문제 난이도 및 난이도 판단 근거, 알고리즘 사용 근거 해설이 렌더링된다. 풀이가 여러 개인 경우 풀이별 탭(각 탭에 해설 + 시간복잡도 포함)으로 구성되며, 현재 활성화된 탭에 해당하는 "사용해야 되는 알고리즘" 버튼이 생성된다. 버튼 클릭 시 기능 A가 해당 알고리즘 이름으로 실행된다.

### 4.1 공통 동작 규칙
- 두 기능 모두 요청이 진행되는 동안 입력 버튼은 비활성화(disable) 처리되어 중복 요청을 막는다.
- 새로운 검색이 실행되면 이전 결과는 사라지고 새 결과로 대체된다 (결과 누적/히스토리 없음, DB 없음 원칙과 일치).
- 알고리즘 검색과 문제 검색 결과 화면은 레이아웃 골격(상태표시줄 → 결과 카드들 → 하단 버튼)을 공유하되, 카드 내용 구성만 다르다.

---

## 5. 예외 처리 (가장 상세한 섹션)

모든 예외는 공통적으로 아래 원칙을 따른다: **(1) 화면이 절대 하얗게 죽지 않는다(No White Screen), (2) 상태 표시줄에 현재 상태를 사람이 이해할 수 있는 문구로 알린다, (3) 가능하면 사용자가 즉시 다시 시도할 수 있는 방법(버튼/안내)을 함께 제공한다.**

### 5.1 빈 입력으로 검색 시도
- **트리거:** 알고리즘/문제 검색 텍스트박스가 비어 있는 상태에서 검색 버튼 클릭 또는 엔터 입력.
- **감지:** 클라이언트 단에서 요청 전송 전 값의 trim() 결과 길이 확인.
- **처리:** AI 요청을 아예 보내지 않는다(불필요한 처리 비용 방지). 상태 표시줄에 "검색어를 입력해주세요" 문구를 경고 색상(예: 노랑/주황 계열)으로 표시. 텍스트박스에 포커스를 자동으로 이동하고 테두리를 강조 색으로 표시.
- **복구:** 사용자가 값을 입력하면 상태 표시줄은 자동으로 idle 상태 문구로 돌아간다.

### 5.2 공백/특수문자만으로 이루어진 무의미한 입력
- **트리거:** 문제 검색란에 공백, 이모지, 특수문자만 반복 입력(예: ".....", "ㅋㅋㅋ" 등)하고 검색.
- **감지:** trim() 후 한글/영문/숫자 등 유효 문자가 일정 비율(예: 최소 1글자) 이상 포함되는지 검사.
- **처리:** "문제 내용을 조금 더 구체적으로 입력해주세요" 상태 문구 표시. AI 호출은 생략하여 무의미한 비용 발생을 막는다.
- **복구:** 사용자가 유효한 문제 내용을 다시 입력하도록 안내.

### 5.3 자동완성 목록에 없는 알고리즘 이름 입력
- **트리거:** 알고리즘 검색란에 사전 정의 목록에 존재하지 않는 이름(오타, 존재하지 않는 알고리즘, 혹은 임의의 단어)을 입력하고 자동완성에서 선택하지 않은 채 바로 검색.
- **감지:** 검색 실행 시점에 입력값이 유효 알고리즘 목록(정적 데이터)의 이름과 정확히 일치하는지 확인.
- **처리:** AI 호출 전에 차단한다. 상태 표시줄에 "일치하는 알고리즘을 찾을 수 없어요. 목록에서 선택해주세요" 문구 표시와 함께, 입력값과 가장 유사한 알고리즘 이름 후보를 자동완성 드롭다운으로 즉시 다시 보여준다(유사도/부분 문자열 매칭).
- **복구:** 후보 중 하나를 클릭하면 정상 검색으로 이어진다.

### 5.4 AI 응답 지연 (느린 응답)
- **트리거:** 요청 후 일정 시간(예: 3~5초) 이상 응답이 오지 않음.
- **감지:** 요청 시작 시각 기준 타이머 설정. 임계 시간 경과 시 상태를 "지연"으로 전환.
- **처리:** 로딩 스피너/스켈레톤은 유지한 채, 상태 표시줄 문구를 "답변을 생성하는 중입니다... 조금만 기다려주세요"에서 "예상보다 시간이 걸리고 있어요. 계속 기다리는 중입니다"로 갱신. 사용자가 불안해하지 않도록 진행 중임을 지속적으로 알림 (예: 점 애니메이션).
- **복구:** 응답이 오면 정상 출력, 아래 5.5의 타임아웃 조건에 도달하면 오류로 전환.

### 5.5 AI 응답 타임아웃 (완전 실패)
- **트리거:** 요청 후 최대 대기 시간(예: 15~20초)을 초과해도 응답이 오지 않음.
- **감지:** 타임아웃 타이머 만료.
- **처리:** 진행 중이던 요청을 취소(abort)하고 로딩 상태를 해제. 상태 표시줄에 "응답 시간이 초과되었습니다. 다시 시도해주세요" 오류 문구(빨간 계열)와 함께 "다시 시도" 버튼을 결과 영역에 노출.
- **복구:** "다시 시도" 버튼 클릭 시 동일 입력값으로 재요청.

### 5.6 AI 처리 중 오류 발생 (네트워크/서버/API 오류)
- **트리거:** 요청이 네트워크 오류, 서버 5xx, API 키/권한 오류 등으로 실패.
- **감지:** 요청의 실패 응답(reject/catch) 처리.
- **처리:** 에러 종류를 사용자에게 노출할 필요 없는 저수준 메시지(스택트레이스 등)는 감추고, 상태 표시줄에 "일시적인 오류가 발생했습니다. 잠시 후 다시 시도해주세요" 문구 표시. "다시 시도" 버튼 제공.
- **복구:** 재시도 버튼 클릭 시 동일 요청 재실행. 반복 실패 시에도 앱은 계속 재시도를 허용한다(횟수 제한 없음, 단 매 시도마다 상태 문구는 갱신).

### 5.7 AI가 형식에 맞지 않는 응답을 반환 (파싱 실패)
- **트리거:** AI 응답이 화면에 매핑해야 할 필드(설명/사용처/예시코드/관련 알고리즘 등)를 갖추지 못했거나 형식이 깨져 파싱이 불가능함.
- **감지:** 응답 파싱 단계에서 필수 필드 존재 여부 검증(스키마 검증).
- **처리:** 파싱 실패 시 원문을 그대로 화면에 뿌리지 않고, 상태 표시줄에 "답변을 정리하는 데 문제가 발생했습니다. 다시 시도해주세요" 표시 후 재시도 유도. 부분적으로라도 파싱 가능한 필드가 있다면(예: 설명은 있는데 예시코드가 없음) 있는 필드만 표시하고 없는 필드는 "이 항목은 준비되지 않았습니다" 플레이스홀더로 대체.
- **복구:** 재시도 버튼 또는 동일 알고리즘 재검색으로 복구.

### 5.8 문제에 매칭되는 알고리즘을 찾지 못한 경우
- **트리거:** 문제 검색 결과, AI가 확신 있게 제시할 수 있는 알고리즘이 없거나 문제 자체가 알고리즘 문제로 판단되지 않음.
- **감지:** AI 응답에서 "매칭 알고리즘 없음"을 명시적으로 나타내는 필드/플래그로 판단.
- **처리:** 오류가 아닌 정상 결과의 한 형태로 취급. 상태 표시줄은 성공(success) 상태를 유지하되, 결과 카드에 "이 문제에 뚜렷하게 대응하는 알고리즘을 찾지 못했어요. 문제 설명을 더 구체적으로 입력해보세요"라는 안내 표시. 이 경우 하단 "사용해야 되는 알고리즘" 버튼은 생성하지 않는다.
- **복구:** 사용자가 문제 설명을 보강하여 재검색.

### 5.9 관련 알고리즘/추천 알고리즘 버튼이 가리키는 대상 데이터가 없는 경우
- **트리거:** "관련된 알고리즘 보기" 또는 "사용해야 되는 알고리즘" 버튼에 표시된 이름이 실제 유효 알고리즘 목록(5.3의 사전 정의 집합)에 없는 이름으로 잘못 생성된 경우.
- **감지:** 버튼 클릭 시점에 해당 이름을 유효 알고리즘 목록과 대조.
- **처리:** 버튼 클릭이 무시되지 않도록, 클릭 시 5.3과 동일한 "일치하는 알고리즘을 찾을 수 없어요" 상태로 전환하고 상단 알고리즘 검색창에 해당 이름을 자동으로 채워 넣어 사용자가 유사 항목을 고를 수 있게 돕는다.
- **복구:** 자동완성 후보 선택으로 복구.

### 5.10 순환 참조로 인한 무한 루프 우려 (A → B → A 관련 알고리즘 반복)
- **트리거:** 관련 알고리즘 보기를 반복 클릭하며 A→B→A로 순환하는 탐색 자체는 정상 사용성이지만, 동일 알고리즘을 즉시 자기 자신의 "관련 알고리즘"으로 반환하는 등 AI 응답 오류로 인한 자기 참조가 발생.
- **감지:** 관련 알고리즘 목록에 현재 조회 중인 알고리즘 이름과 동일한 항목이 포함되어 있는지 필터링.
- **처리:** 자기 자신을 가리키는 관련 알고리즘 버튼은 화면에 생성하지 않는다(자동 제거). 남는 관련 알고리즘이 하나도 없으면 "관련된 알고리즘이 없습니다" 문구를 버튼 대신 표시.
- **복구:** 별도 사용자 조치 불필요(자동 정제).

### 5.11 중복 클릭 / 연타로 인한 중복 요청
- **트리거:** 검색 버튼이나 관련 알고리즘 버튼을 로딩 중에 연속 클릭.
- **감지:** 현재 요청 진행 상태(loading) 플래그.
- **처리:** loading 상태인 동안 모든 검색/버튼 클릭을 비활성화(disabled) 처리하여 추가 요청 자체가 발생하지 않도록 원천 차단.
- **복구:** 진행 중이던 요청이 끝나면 버튼이 다시 활성화된다.

### 5.12 네트워크 연결 끊김 (오프라인)
- **트리거:** 요청 시점에 기기가 인터넷에 연결되어 있지 않음.
- **감지:** 브라우저의 온라인/오프라인 상태 이벤트, 또는 요청 실패 시 네트워크 오류 유형 확인.
- **처리:** 상태 표시줄에 "인터넷 연결을 확인해주세요" 문구와 오프라인 아이콘 표시. 재연결 감지 시 자동으로 안내 문구를 idle로 되돌린다(자동 재요청은 하지 않음, 사용자가 직접 재시도).
- **복구:** 사용자가 연결 복구 후 다시 시도 버튼 클릭.

### 5.13 매우 긴 입력값 (글자 수 초과)
- **트리거:** 문제 검색란에 지나치게 긴 텍스트(예: 2,000자 이상)를 붙여넣기 함.
- **감지:** 입력값 길이 실시간 체크, 제한(예: 500~1000자) 초과 여부 판단.
- **처리:** 제한 글자 수 초과 시 그 이상 입력을 막거나(입력 자체를 자르지 않고 경고), 텍스트박스 하단에 "OOO/1000자" 카운터와 함께 "문제 설명은 1000자 이내로 입력해주세요" 경고를 표시하고 검색 버튼을 비활성화한다.
- **복구:** 사용자가 글자 수를 줄이면 버튼이 다시 활성화된다.

### 5.14 악의적 입력 / 스크립트 삽입 시도 (XSS 방지)
- **트리거:** 입력값에 `<script>`, HTML 태그 등이 포함되어 있음.
- **감지:** 입력값을 화면에 렌더링하기 전 항상 이스케이프 처리(텍스트로만 렌더링, HTML로 해석하지 않음).
- **처리:** 입력값은 그대로 받아 AI 요청 텍스트로는 사용하되, 화면에 표시될 때는 순수 텍스트로만 렌더링되어 실행되지 않는다. 별도 오류 문구 없이 조용히 안전하게 무력화한다.
- **복구:** 해당 없음 (사용자 인지 불필요한 내부 방어 로직).

### 5.15 결과 복사 기능 실패 (클립보드 접근 권한 없음)
- **트리거:** 복사 아이콘 클릭 시 브라우저/기기의 클립보드 접근 권한이 없거나 API를 지원하지 않는 환경.
- **감지:** 클립보드 API 호출 실패(reject) 감지.
- **처리:** 아이콘 근처에 짧게(토스트 형태) "복사에 실패했습니다. 직접 선택해 복사해주세요" 안내 표시.
- **복구:** 사용자가 텍스트를 직접 드래그하여 복사.

### 5.16 복사 성공 피드백 누락 방지 (참고: 정상 흐름 보강)
- **트리거:** 복사가 정상적으로 이루어졌으나 사용자가 성공 여부를 알 수 없어 반복 클릭.
- **감지:** 클립보드 API 성공 콜백.
- **처리:** 복사 성공 시 아이콘이 일시적으로(예: 1.5초) 체크 아이콘으로 바뀌거나 "복사됨" 툴팁을 표시해 중복 클릭을 방지한다.
- **복구:** 해당 없음(사용성 개선 차원의 처리).

### 5.17 모바일 환경에서의 레이아웃/입력 이슈
- **트리거:** 작은 화면 폭에서 자동완성 드롭다운이 화면 밖으로 넘치거나, 가상 키보드가 입력창을 가리는 경우.
- **감지:** 반응형 브레이크포인트 기준 뷰포트 폭 감지.
- **처리:** 자동완성 드롭다운의 최대 높이를 제한하고 내부 스크롤 처리, 입력창 포커스 시 해당 영역이 가상 키보드에 가려지지 않도록 자동 스크롤. 모든 버튼/터치 영역은 최소 터치 크기(약 44px)를 보장한다.
- **복구:** 해당 없음(레이아웃 자체 보정).

### 5.18 상태 표시줄 우선순위 규칙 (예외 간 충돌 방지)
- 여러 예외가 동시에 발생할 수 있는 경우(예: 오프라인 상태에서 빈 입력으로 검색), 우선순위는 다음과 같다: **1순위: 클라이언트 측 입력 검증 오류(5.1~5.3) → 2순위: 네트워크 연결 문제(5.12) → 3순위: 요청 처리 중 상태(로딩/지연, 5.4) → 4순위: 서버/AI 처리 오류(5.5~5.7)**. 항상 하나의 상태 문구만 표시하여 사용자가 혼란스럽지 않도록 한다.

### 5.19 특정 언어의 예시 코드만 누락되거나 생성에 실패한 경우
- **트리거:** AI 응답에서 일부 언어(예: Java)의 예시 코드만 비어 있거나 형식이 깨져 파싱할 수 없음(다른 언어는 정상).
- **감지:** 언어별 코드 필드를 각각 개별적으로 파싱·존재 여부 검증.
- **처리:** 정상 파싱된 언어 탭은 그대로 노출하고, 실패한 언어 탭만 비활성화하거나 탭 내부에 "이 언어의 예시 코드를 불러오지 못했습니다" 플레이스홀더와 재시도 아이콘을 표시한다. 일부 언어가 실패했다고 카드 전체를 오류로 처리하지 않는다(부분 성공 허용).
- **복구:** 사용자가 실패한 언어 탭 안의 재시도 아이콘을 클릭하면 그 언어의 예시 코드만 다시 요청한다.

---

## 6. 완료 조건 (Done Criteria)

1. [알고리즘 검색] 탭에서 유효한 알고리즘을 입력하고 검색(버튼 클릭 또는 엔터)하면, 설명·난이도(및 판단 근거)·사용처 및 적용 문제·예시 코드(C/C++/Java/Python 등 언어 탭으로 전환 가능)가 출력되고 관련 알고리즘 보기 버튼이 생성된다.
2. [문제 검색] 탭에서 문제를 입력하고 검색하면, 문제 설명·문제 난이도(및 판단 근거)·알고리즘 사용 근거 해설이 출력된다. 풀이가 여러 개인 경우 풀이별 탭으로 모두 확인할 수 있고 각 탭에 시간복잡도가 표시되며, 현재 활성화된 탭에 해당하는 "사용해야 되는 알고리즘" 버튼이 생성되어 클릭 시 1번과 동일한 알고리즘 상세 화면([알고리즘 검색] 탭)으로 전환된다.
3. "관련된 알고리즘 보기" 버튼을 클릭하면 화면이 [알고리즘 검색] 탭으로 전환되며 해당 알고리즘에 대해 1번과 동일한 결과 화면이 다시 나타난다 (반복 탐색 가능).
4. 빈 입력, 목록에 없는 알고리즘, 무의미한 문제 텍스트, 응답 지연, 응답 타임아웃, 서버/네트워크 오류, 파싱 실패, 매칭 알고리즘 없음, 오프라인, 글자 수 초과, 클립보드 실패 등 5장에 정의된 모든 예외 상황에서 상태 표시줄에 현재 상태가 정확히 표시된다.
5. 처리 중(로딩)일 때는 상태 표시줄 아래 결과 출력부와 하단 버튼 영역 전체가 로딩 표시로 덮인다.
6. 어떤 예외 상황에서도 화면이 하얗게 멈추거나(White Screen) 자바스크립트 오류로 전체 UI가 응답 불가 상태가 되지 않는다 — 항상 상태 표시줄과 입력 UI는 살아있어 사용자가 재시도할 수 있다.
7. 로그인/결제/데이터베이스/파일 업로드/결과 저장/공유 관련 UI나 로직이 화면 어디에도 존재하지 않는다.
8. 각 결과 카드의 복사 아이콘 클릭 시 해당 텍스트가 클립보드에 정상 복사되거나(성공 피드백 표시), 실패 시 5.15에 정의된 안내가 표시된다.